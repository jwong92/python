# You can import a number of modules using
# for importing random numbers
import random 
import sys
import os

# Once tuples are made, they can't be changed (unlike lists)

# create a tuple - create because you have data you don't want easily changed
pi_tuple = (3,1,4,1,5,9)

# make tuple into a list
new_tuple = list(pi_tuple)

# convert list into a tuple
new_list = tuple(new_tuple)

# get length of tuple
len(tuple)
min(tuple)
max(tuple)